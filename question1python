class Solution(object):
    def twoSum(self, nums, target):
        i = 0
        while i != (len(nums)):
            test = target - nums[i]
            if test in nums:
                find = nums.index(test)
                idex = i
                if find != i or None:
                    return i, find
                    break
            i += 1
