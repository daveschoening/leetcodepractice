class Solution(object):
    def twoSum(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """
        i = 0
        while i != (len(nums)):
            print("i", i)
            test = target - nums[i]
            if test in nums:
                find = nums.index(test)
                idex = i
                print("idex",idex)
                print("find",find)
                if find != i or None:
                    return i, find
                    break
            i += 1
